/* import { useEffect } from 'react'
import axios from 'axios';

const GetDiary = () => {
    const res = {};
     useEffect(() => {
        axios
            .get('/react/list')
            .then(({ data }));
    }, []);

    return (res)
}

export default GetDiary
 */