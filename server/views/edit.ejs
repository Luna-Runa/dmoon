<!doctype html>
<html lang="kr">

<head>
  <%- include('header.ejs') %>
</head>

<body>
  <%- include('diaryNavbar.html') %>

    <!-- <div class="container mt-4">
      <h4 class="ml-2 my-3">수정</h4>
      <form action="/edit?_method=PUT" method="POST">
        <div class="card" style="width: 100%;">
          <div class="card-body">
            <div class="form-group" name="mood">
              <div class="btn-group mb-3" role="group">
                <input type="radio" class="btn-check" name="mood" id="mood1" value="행복함" autocomplete="off">
                <label class="btn btn-outline-primary" for="mood1">행복함</label>

                <input type="radio" class="btn-check" name="mood" id="mood2" value="즐거움" autocomplete="off">
                <label class="btn btn-outline-primary" for="mood2">즐거움</label>

                <input type="radio" class="btn-check" name="mood" id="mood3" value="보통" autocomplete="off">
                <label class="btn btn-outline-primary" for="mood3">보통</label>

                <input type="radio" class="btn-check" name="mood" id="mood4" value="그저그럼" autocomplete="off">
                <label class="btn btn-outline-primary" for="mood4">그저그럼</label>

                <input type="radio" class="btn-check" name="mood" id="mood5" value="기분나쁨" autocomplete="off">
                <label class="btn btn-outline-primary" for="mood5">기분나쁨</label>
              </div>
            </div>

            <h6 class="card-subtitle mb-2 text-muted">
              <%= data.date%>
            </h6>

            <p class="text-justify mt-3">한 일들</p>

            <div class="input-group mb-3">
              <div class="input-group-text">
                <input class="form-check-input mt-0" type="checkbox" name="todoBool" <%=data.todoBool? "checked" : '' ;
                  %>>
              </div>
              <input type="text" class="form-control" name="todoText" value="<%=data.todoText%>">
            </div>

            <input type="text" value="<%= data._id %>" name="id" style="display: none;">

            <button type="button" class="btn btn-primary">수정</button>
          </div>
        </div>
      </form>
    </div> -->

    <form action="/edit?_method=PUT" method="POST"> 
      <input type="text" value="<%= data._id %>" name="id" style="display: none;">
      <button type="submit">Delete</button>
    </form>


    <!-- jquery 불러오기 -->
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>

    <script>
      //받아온 data를 불러와서 기본값 처리
      let temp = "<%=data.mood %>";

      $(":radio[name='mood'][value='" + temp + "']").attr('checked', true);

      let mood = $('input[name=mood]:checked').val();
      let todoBool = e.target.dataset.id

      $("#put").click(function () {
            $.ajax({
                url: "/edit",
                method: "PUT",
                data: {},
                success: function (data) {
                    $('#output').val(data)
                    console.log(data)
                }
            })
        })
    </script>

    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"
      integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49"
      crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"
      integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy"
      crossorigin="anonymous"></script>

</body>

</html>